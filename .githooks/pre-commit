#!/bin/sh

if ! command -v bun >/dev/null 2>&1; then
  echo "Bun is not installed. Please install Bun first." >&2
  exit 1
fi

if ! bun --silent run tools/dev/quiet.ts -- biome check --write; then
  echo "Biome check failed. Commit aborted." >&2
  exit 1
fi

exit 0
