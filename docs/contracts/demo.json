{
  "name": "Production Demo",
  "access": {
    "anon": "Demo mode is not shipped as a public product surface yet.",
    "user": "Ordinary signed-in sessions are never converted to demo mode.",
    "admin": "Admin capabilities are never simulated.",
    "agent": "Demo mode is unavailable to OAuth and MCP clients."
  },
  "rules": {
    "planned-only": "Demo authentication is a planned product experiment. Implementation details, bootstrap URLs, kill switches, and overlay formats stay out of public docs until the feature ships.",
    "no-live-writes": "Any future demo mode must not write business rows or impersonate real users.",
    "ordinary-principals-unchanged": "Anonymous, authenticated, admin, GraphQL, REST, OAuth, and MCP behavior outside an explicit demo principal remains unchanged."
  },
  "capabilities": {
    "demo-bootstrap": {
      "title": "Demo Bootstrap",
      "auth": "anon",
      "web": "planned",
      "rest": "unavailable",
      "graphql": "unavailable",
      "mcp": "unavailable",
      "display": {
        "fields": [
          "Persistent demo-mode banner when active",
          "Clear exit-demo action",
          "No demo credential on the sign-in page"
        ]
      }
    }
  }
}
