{
  "name": "Webhook Login",
  "access": {
    "anon": "Not a public product entry point.",
    "user": "Not applicable.",
    "admin": "Internal ops only when explicitly enabled in a private environment.",
    "agent": "Not applicable."
  },
  "rules": {
    "not-product": "Webhook login is an internal debugging aid, not a Life@USTC product capability. Do not document enablement flags, paths, or rate limits in public materials.",
    "no-session-in-body": "When used, the session token must never appear in JSON response bodies."
  },
  "capabilities": {
    "webhook-login": {
      "title": "Webhook Login",
      "auth": "admin",
      "web": "unavailable",
      "rest": "unavailable",
      "graphql": "unavailable",
      "mcp": "unavailable",
      "display": {
        "fields": [
          "Internal debugging aid only",
          "Session established via cookie; never returned in JSON bodies"
        ]
      }
    }
  }
}
