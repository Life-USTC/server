{
  "name": "Admin",
  "access": {
    "anon": "Cannot access the admin backend.",
    "user": "Cannot access the admin backend.",
    "admin": "Can access governance pages for moderation, users, OAuth, bus, and more. Active suspensions block admin mutations but not read-only admin pages/list endpoints.",
    "agent": "No admin tools are currently provided."
  },
  "rules": {
    "divided-by-responsibility": "The admin backend is divided by governance responsibility, not all actions on one page.",
    "moderation-centralized": "Comment and description governance are currently centralized on the moderation page.",
    "high-risk-feedback": "High-risk admin actions such as delete, suspend, and hide must have clear feedback.",
    "admin-self-protection": "Admin user management must reject self-suspension, self-demotion, and removing the final remaining admin.",
    "admin-rest-session-only": "Admin REST endpoints require the in-site Better Auth session cookie and do not accept OAuth bearer tokens.",
    "paginated-moderation-lists": "Admin comments, descriptions, homeworks, and users use the shared { data, pagination } list envelope and accept page/pageSize with the deprecated limit alias.",
    "suspended-admin-write-gate": "Admins with an active suspension may load read-only admin pages and list endpoints, but admin REST mutations and page actions are blocked with the standard suspended 403 response until the suspension is lifted.",
    "suspension-expiration-validation": "Suspension expiration accepts omitted, null, or empty input as permanent and rejects invalid non-empty date strings.",
    "single-open-suspension": "Each user can have at most one open suspension. Creating a new suspension closes any previous open suspension for that user while retaining history; lifting an open suspension clears the user's open suspension state.",
    "admin-mutation-not-found": "PATCH /api/admin/comments/[id], POST /api/admin/suspensions, and PATCH /api/admin/users/[id] return 404 when the target record is not found.",
    "suspension-retention": "Suspension history is retained when an admin account is deleted; creator and lifter actor references may be null after account deletion.",
    "primary-admin-navigation": "The global shell exposes each admin tool (users, moderation, OAuth, bus, operations and issues, usage statistics) under Admin tools. /admin redirects to /admin/users; resource URLs remain stable.",
    "audit-session-only": "The /admin/audit and /admin/analytics pages require an in-site Better Auth admin session. OAuth bearer tokens cannot access governance data.",
    "audit-safe-projection": "The audit rows use stable (createdAt,id) cursor pagination and expose allowlisted structured metadata only; content bodies, IP addresses, full user agents, tokens, secrets, grant IDs, session IDs, and request IDs are not displayed in audit rows. The separate feature issue panel may display validated safe request IDs for correlation, without raw exceptions or stacks.",
    "aggregate-only-analytics": "Historical activity combines first-party audit events and content-free OAuth daily usage, excluding external OAuth mutation audit rows to avoid double counting. Daily operation and access-method curves can be narrowed by feature. OAuth history identifies read/write only, never inferred specific operations. This source remains separate from new feature observations.",
    "feature-experience-telemetry": "Platform-owned PostgreSQL FeatureOperationEvent supplies unsampled recorded counts by Shanghai day, feature, operation, protocol, surface, known authentication mode, and outcome. The current day is partial. Database percentiles describe recorded operation wall duration. Summary and daily totals include all matching rows even when the expanded matrix is limited to 500 cells. Public reads are observed without extra authentication queries; known user IDs come only from existing verified principals. Identified active users are distinct known users, not anonymous visitors.",
    "feature-experience-outcomes": "Uninspected successful SvelteKit page data envelopes have unknown application outcomes and no identified error. They contribute to usage but not issue samples or warning logs.",
    "feature-experience-retention": "Feature observations and safe RuntimeIssueEvent warnings/errors are persisted in a bounded background batch (maximum 256 events per request), outside the business transaction, using PostgreSQL without an Analytics Engine read API, token, or binding. Database failure, process termination, and overflow can lose observations; recorded counts are not a guarantee of all traffic. No historical backfill or zeros before first observation. Events have bounded 90-day retention; each maintenance run deletes bounded batches. Logs contain structured event names, status and safe request IDs, not request bodies, search text, arbitrary paths, messages, IP addresses or raw exception stacks.",
    "responsive-workspace": "Admin management pages share a lightweight workspace order of page identity, optional feedback or filters, then records; desktop tables retain their density while existing compact record views remain the mobile presentation.",
    "platform-operation-browser": "The operations page offers issue-only or all recorded feature operations, filters by feature, operation, protocol, outcome and known actor, grouped issue counts, daily trends and deterministic (occurredAt,id) cursor pagination. Security audit records remain a separate source with their existing safe projection. Runtime warnings/errors are shown separately as the latest 20 records in the selected time window."
  },
  "capabilities": {
    "admin-entry": {
      "title": "Admin Entry",
      "auth": "admin",
      "web": {
        "pages": [
          "/admin"
        ]
      },
      "display": {
        "fields": [
          "Redirects to user management",
          "Primary sidebar links for moderation, users, OAuth, bus, audit, and analytics"
        ]
      }
    },
    "moderation": {
      "title": "Moderation",
      "auth": "admin",
      "web": {
        "pages": [
          "/admin/moderation"
        ]
      },
      "rest": {
        "routes": [
          {
            "path": "/api/admin/comments",
            "returns": "{ data: Comment[], pagination }"
          },
          {
            "path": "/api/admin/comments/[id]",
            "method": "PATCH",
            "reference_only": true
          },
          {
            "path": "/api/admin/descriptions",
            "returns": "{ data: Description[], pagination }"
          },
          {
            "path": "/api/admin/descriptions/[id]",
            "method": "PATCH",
            "reference_only": true
          },
          {
            "path": "/api/admin/suspensions",
            "returns": "{ data: Suspension[] }"
          },
          {
            "path": "/api/admin/suspensions",
            "method": "POST",
            "reference_only": true
          },
          {
            "path": "/api/admin/suspensions/[id]",
            "method": "PATCH",
            "reference_only": true
          },
          {
            "path": "/api/admin/homeworks",
            "returns": "{ data: HomeworkItem[], pagination }"
          },
          {
            "path": "/api/admin/homeworks/[id]",
            "method": "DELETE",
            "reference_only": true
          }
        ]
      },
      "mcp": "unavailable",
      "display": {
        "fields": [
          "Comment moderation table",
          "Description moderation table",
          "content/descriptionPreview",
          "author / lastEditedBy",
          "postedIn target",
          "timestamp",
          "comment status",
          "detail dialog controls"
        ]
      }
    },
    "user-management": {
      "title": "User Management",
      "auth": "admin",
      "web": {
        "pages": [
          "/admin/users"
        ]
      },
      "rest": {
        "routes": [
          {
            "path": "/api/admin/users",
            "returns": "{ data: User[], pagination }"
          },
          {
            "path": "/api/admin/users/[id]",
            "method": "PATCH",
            "returns": "{ user: User }",
            "notes": [
              "Returns 404 when the target user is not found."
            ]
          }
        ]
      },
      "mcp": "unavailable",
      "display": {
        "fields": [
          "user.email",
          "user.name",
          "user.username",
          "user.createdAt",
          "Is admin checkbox",
          "Actions: edit profile/admin state, suspend/unsuspend"
        ]
      }
    },
    "oauth-client-management": {
      "title": "OAuth Client Management",
      "auth": "admin",
      "web": {
        "pages": [
          "/admin/oauth"
        ]
      },
      "rest": "unavailable",
      "mcp": "unavailable",
      "display": {
        "fields": [
          "client.name and client.clientId",
          "client trust and fixed authentication type",
          "client enabled/disabled status",
          "client.scopes[] summary",
          "client.createdAt",
          "Empty inventory explanation and create-client action",
          "Create: name, explicit redirect URIs, scopes, and one of three fixed client patterns",
          "Client secret is shown once after creation and never in the inventory",
          "Action: confirmed delete"
        ]
      }
    },
    "bus-management": {
      "title": "Bus Management",
      "auth": "admin",
      "web": {
        "pages": [
          "/admin/bus"
        ]
      },
      "rest": "unavailable",
      "mcp": "unavailable",
      "display": {
        "fields": [
          "Bus schedule version title",
          "version key",
          "tripCount",
          "weekday, Saturday, and Sunday service-day trip data",
          "importedAt",
          "effective range",
          "enabled status",
          "Actions: import, activate, delete"
        ]
      }
    },
    "audit-log": {
      "title": "Operations and Issues",
      "auth": "admin",
      "web": {
        "pages": [
          "/admin/audit"
        ]
      },
      "rest": "unavailable",
      "mcp": "unavailable",
      "display": {
        "fields": [
          "createdAt",
          "actor and subject summaries",
          "OAuth client name/id",
          "action, outcome, channel",
          "target type/id",
          "allowlisted structured metadata",
          "separately bounded feature issue samples, including today: safe request ID, timestamp, outcome, and error class"
        ],
        "filters": [
          "actor",
          "subject",
          "client",
          "action",
          "outcome",
          "channel",
          "target type/id",
          "date range"
        ]
      }
    },
    "aggregate-analytics": {
      "title": "Usage Statistics",
      "auth": "admin",
      "web": {
        "pages": [
          "/admin/analytics"
        ]
      },
      "rest": "unavailable",
      "mcp": "unavailable",
      "display": {
        "fields": [
          "feature",
          "channel",
          "outcome",
          "client",
          "count",
          "time window: 7, 30, or 90 days",
          "Shanghai-day window: 7, 30, or 90 days including partial today",
          "feature × operation × protocol × surface × authentication mode × outcome matrix",
          "recorded totals, rejected, errors, unknown outcomes and identified active users",
          "PostgreSQL wall-time p50 and p95 for recorded operations",
          "available cells reflect observed telemetry only",
          "coverage start, recording limits, retention, and identified-user definition"
        ],
        "filters": [
          "feature",
          "operation",
          "protocol",
          "surface",
          "authentication mode",
          "outcome"
        ]
      }
    }
  }
}
