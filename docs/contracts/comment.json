{
  "name": "Comment",
  "access": {
    "anon": "Can read public comments but cannot create sign-in-required comments.",
    "user": "Can post, reply, react, edit, and delete their own comments; suspended users can read permitted comments but cannot use comment write actions or upload comment attachments.",
    "admin": "Unsuspended admins can hide, delete, and moderate comments through moderation surfaces, and handle suspensions.",
    "agent": "Can read visible comment lists, focused threads, and ordinary signed-in user comment write actions through MCP with authenticated viewer context; no admin moderation capability is provided."
  },
  "rules": {
    "attached-to-object": "Comments must be attached to a specific object; no decontextualized public feed entry is provided.",
    "attached-object-types": "Attached objects include courses, sections, teachers, homework, and section-teacher relationships.",
    "rich-content": "Supports Markdown, math formulas, emoji, tables, replies, and reactions.",
    "server-rendered-markdown": "Visible comment nodes include body as the source Markdown and renderedBody as sanitized server-rendered HTML. Web read surfaces consume renderedBody without rerunning the Markdown pipeline during hydration; REST and MCP full mode keep the shared serialized comment-node shape, while MCP compact modes omit renderedBody to avoid repeating the source Markdown in AI context.",
    "markdown-font-csp": "Production builds emit Markdown and KaTeX font files as same-origin assets instead of data URIs so every required math glyph can load under the configured font-src Content Security Policy.",
    "visibility-modes": "Supports public and signed-in-only audience visibility; anonymous posting is represented by comment.isAnonymous and hides identity from regular users while remaining traceable by admins in governance scenarios.",
    "shared-read-policy": "Web, REST, and MCP comment reads share the same target resolution, thread serialization, visibility filtering, author masking, reaction, attachment, and viewer action-flag logic; read-only paths must not create durable comment targets and valid empty section-teacher targets load as empty threads.",
    "thread-pagination": "Comment list pagination counts viewer-visible root threads and selects roots by createdAt then id in ascending order before returning each selected root with its complete reply tree. The existing verified-author-first display ordering is applied within each returned page. REST uses page/pageSize with deprecated limit compatibility; MCP uses page/limit. Pagination never severs replies from their root thread, and meta.hiddenCount remains a target-wide count for anonymous viewers.",
    "interaction-gate": "Edits, deletes, replies, and reaction changes require an authenticated, unsuspended viewer and an active visible comment target; deleted or softbanned comments must not expose canEdit/canDelete/canReply/canReact or accept those write actions. Admin moderation affordances require an unsuspended admin."
  },
  "capabilities": {
    "object-comment-section": {
      "title": "Object Comment Section",
      "auth": "anon",
      "web": "stable",
      "graphql": {
        "mutations": [
          {
            "name": "commentCreate",
            "arguments": { "input": "CreateCommentInput!" },
            "returns": "CommentMutationPayload!",
            "auth": "user",
            "required_scopes": ["comment:write"],
            "rest_equivalent": "POST /api/community/comments",
            "mcp_equivalent": "community_comment_create"
          },
          {
            "name": "commentUpdate",
            "arguments": {
              "id": "ID!",
              "input": "UpdateCommentInput!"
            },
            "returns": "CommentMutationPayload!",
            "auth": "user",
            "required_scopes": ["comment:write"],
            "rest_equivalent": "PATCH /api/community/comments/[id]",
            "mcp_equivalent": "community_comment_update"
          },
          {
            "name": "commentDelete",
            "arguments": { "id": "ID!" },
            "returns": "DeleteMutationPayload!",
            "auth": "user",
            "required_scopes": ["comment:write"],
            "rest_equivalent": "DELETE /api/community/comments/[id]",
            "mcp_equivalent": "community_comment_delete"
          },
          {
            "name": "commentsDelete",
            "arguments": { "ids": "[ID!]!" },
            "returns": "CommentDeleteBatchPayload!",
            "auth": "user",
            "required_scopes": ["comment:write"],
            "rest_equivalent": "DELETE /api/community/comments/batch",
            "mcp_equivalent": "graphql_operation_run",
            "notes": [
              "Registered operation id: community.comments.delete.v1.",
              "Accepts up to 50 unique comment ids and returns stable per-item success, not_found, forbidden, or locked results.",
              "Each successful item uses the same permission, suspension, interaction-lock, transaction, and audit path as the single-comment delete operation."
            ]
          },
          {
            "name": "commentReactionAdd",
            "arguments": {
              "commentId": "ID!",
              "type": "CommentReactionType!"
            },
            "returns": "CommentReactionMutationPayload!",
            "auth": "user",
            "required_scopes": ["comment:write"],
            "rest_equivalent": "POST /api/community/comments/[id]/reactions",
            "mcp_equivalent": "community_comment_reaction_add"
          },
          {
            "name": "commentReactionRemove",
            "arguments": {
              "commentId": "ID!",
              "type": "CommentReactionType!"
            },
            "returns": "CommentReactionMutationPayload!",
            "auth": "user",
            "required_scopes": ["comment:write"],
            "rest_equivalent": "DELETE /api/community/comments/[id]/reactions",
            "mcp_equivalent": "community_comment_reaction_remove"
          }
        ]
      },
      "rest": {
        "routes": [
          {
            "path": "/api/community/comments",
            "returns": "{ data: Comment[] (including body and renderedBody), pagination, meta: { hiddenCount: Int, viewer, target } }",
            "notes": [
              "Accepts REST-compatible internal targetId plus public identifiers such as sectionJwId, courseJwId, teacherId, homeworkId, and sectionTeacherId.",
              "Returns 404 when the attached target entity does not exist.",
              "Accepts page and pageSize; deprecated limit remains an alias for pageSize."
            ]
          },
          {
            "path": "/api/community/comments",
            "method": "POST",
            "status": 201,
            "returns": "{ id: String }",
            "auth": "user",
            "notes": [
              "Accepts REST-compatible internal targetId plus public identifiers such as sectionJwId, courseJwId, teacherId, homeworkId, and sectionTeacherId.",
              "visibility accepts only public or logged_in_only; use isAnonymous for identity hiding.",
              "Provided sectionJwId and courseJwId values must be valid positive integers; malformed values return 400 even when targetId is also supplied.",
              "Returns 400 Invalid attachments when any requested upload is missing, owned by another user, or already attached to a comment."
            ]
          },
          {
            "path": "/api/community/comments/[id]",
            "method": "PATCH",
            "returns": "{ success: Boolean, comment: Comment }",
            "auth": "user",
            "notes": [
              "Only the comment owner may use the public edit endpoint; admins moderate through /api/admin/comments/[id].",
              "visibility accepts only public or logged_in_only; use isAnonymous for identity hiding.",
              "Returns 400 Invalid attachments when any requested upload is missing, owned by another user, or already attached to a different comment."
            ]
          },
          {
            "path": "/api/community/comments/[id]",
            "method": "DELETE",
            "returns": "{ success: Boolean }",
            "auth": "user"
          },
          {
            "path": "/api/community/comments/batch",
            "method": "DELETE",
            "returns": "{ results: Array<{ success: true, id: String } | { success: false, id: String, error: { code: 'not_found' | 'forbidden' | 'locked', message: String } }> }",
            "auth": "user",
            "notes": [
              "Accepts up to 50 comment ids.",
              "Deletes each comment using the same owner/locked checks as DELETE /api/community/comments/[id].",
              "Returns per-item results; failures do not stop processing of other ids."
            ]
          },
          {
            "path": "/api/community/comments/[id]/reactions",
            "method": "POST",
            "returns": "{ success: Boolean }",
            "auth": "user"
          },
          {
            "path": "/api/community/comments/[id]",
            "returns": "{ thread: Comment[], focusId: String, hiddenCount: Int, viewer, target }",
            "notes": [
              "Returns 403 when the focused comment exists but is not visible to the viewer."
            ]
          },
          {
            "path": "/api/community/comments/[id]/reactions",
            "method": "DELETE",
            "returns": "{ success: Boolean }",
            "auth": "user"
          }
        ]
      },
      "mcp": {
        "tools": [
          {
            "name": "community_comment_list",
            "returns": "{ found: Boolean, data: Comment[], pagination, meta: { hiddenCount: Int, viewer, target } }",
            "auth": "user",
            "rest_equivalent": "GET /api/community/comments",
            "notes": [
              "Accepts page and limit and preserves complete reply trees for each paged root comment."
            ]
          },
          {
            "name": "community_comment_get",
            "returns": "{ found: Boolean, thread: Comment[], focusId: String, hiddenCount: Int, viewer, target }",
            "auth": "user",
            "rest_equivalent": "GET /api/community/comments/[id]"
          },
          {
            "name": "community_comment_create",
            "returns": "{ success: Boolean, id: String }",
            "auth": "user",
            "rest_equivalent": "POST /api/community/comments",
            "notes": [
              "Uses the same target resolution, visibility validation, isAnonymous identity-hiding flag, and attachment validation as REST; MCP audit metadata records source=mcp."
            ]
          },
          {
            "name": "community_comment_update",
            "returns": "{ success: Boolean, comment: Comment }",
            "auth": "user",
            "rest_equivalent": "PATCH /api/community/comments/[id]",
            "notes": [
              "Only the comment owner may edit through this ordinary-user MCP tool; visibility validation and isAnonymous match REST, and moderation remains unavailable through MCP."
            ]
          },
          {
            "name": "community_comment_delete",
            "returns": "{ success: Boolean }",
            "auth": "user",
            "rest_equivalent": "DELETE /api/community/comments/[id]"
          },
          {
            "name": "community_comment_reaction_add",
            "returns": "{ success: Boolean, changed: Boolean }",
            "auth": "user",
            "rest_equivalent": "POST /api/community/comments/[id]/reactions"
          },
          {
            "name": "community_comment_reaction_remove",
            "returns": "{ success: Boolean, changed: Boolean }",
            "auth": "user",
            "rest_equivalent": "DELETE /api/community/comments/[id]/reactions"
          }
        ]
      },
      "display": {
        "fields": [
          "comment.id",
          "comment.author.name",
          "comment.author.image (avatar)",
          "comment.body (markdown-rendered)",
          "comment.renderedBody (sanitized server-rendered HTML)",
          "comment.createdAt (timestamp)",
          "comment.updatedAt / edited timestamp",
          "comment.visibility badges",
          "comment.status (non-active states)",
          "comment.reactions[] (counts per reaction type)",
          "comment.replies[] (threaded)",
          "comment.attachments[] filename/open action for signed-in viewers with attachment access",
          "canReply/canReact/canEdit/canDelete/canModerate actions",
          "Copy link action"
        ]
      }
    },
    "comment-governance": {
      "title": "Comment Governance",
      "auth": "admin",
      "web": {
        "pages": ["/admin/moderation"]
      },
      "rest": {
        "routes": [
          {
            "path": "/api/admin/comments",
            "returns": "{ data: Comment[], pagination }",
            "notes": [
              "Omitted status defaults to the active moderation queue, matching /admin/moderation.",
              "Accepts page and pageSize; deprecated limit remains an alias for pageSize."
            ]
          },
          {
            "path": "/api/admin/comments/[id]",
            "method": "PATCH",
            "returns": "{ comment: Comment }"
          },
          {
            "path": "/api/admin/suspensions",
            "method": "POST",
            "status": 201,
            "returns": "{ suspension: Suspension }"
          },
          {
            "path": "/api/admin/suspensions/[id]",
            "method": "PATCH",
            "returns": "{ suspension: Suspension }"
          }
        ]
      },
      "mcp": "unavailable",
      "display": {
        "fields": [
          "comment.body/content preview",
          "comment.author.name",
          "target object",
          "creation timestamp",
          "Status: active/softbanned/deleted",
          "Suspended filter",
          "Status radio + confirm action",
          "Suspension controls"
        ]
      }
    },
    "comment-auxiliary-pages": {
      "title": "Comment Auxiliary Pages",
      "auth": "anon",
      "web": {
        "pages": ["/community/comments/[id]", "/guides/markdown-support"]
      },
      "display": {
        "fields": ["Comment guidelines/help pages"]
      }
    }
  }
}
