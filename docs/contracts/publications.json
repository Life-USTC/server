{
  "name": "Publications / Crawler Ingestion",
  "access": {
    "anon": "Cannot write or inspect crawler ingestion data.",
    "user": "Cannot write crawler ingestion data unless the user is an administrator.",
    "admin": "May ingest publications only through an authenticated REST request; the use-case always verifies the administrator flag.",
    "agent": "A crawler uses an administrator-pre-registered OAuth client and the publication.ingest:write scope. This is a REST-only crawler exception and is not a Web, GraphQL, or MCP capability."
  },
  "rules": {
    "rest-only": "Publication ingestion and object upload planning are exposed only under /api/ingestion/publications. Do not add Web, GraphQL, or MCP transports for these writes, and do not treat publication.ingest:write as an MCP scope.",
    "scope-boundary": "publication.ingest belongs to the REST feature registry but is excluded from PUBLIC_REST_FEATURES, public OAuth discovery, dynamic client registration, and MCP. Only an OAuth client created by an administrator can request it; the authenticated principal must still be an administrator.",
    "source-registration": "Every batch carries source descriptors. A descriptor may be the full id/name/organizationLevel/allowedHosts/seedUrls form or the minimal id/name form. The server upserts descriptors in the same transaction, reuses persisted host restrictions for minimal descriptors, then accepts an item only when its sourceId is present in that batch and is enabled and not discovery-only. Canonical URLs must match an allowed source host and not match a blocked host when restrictions are configured.",
    "identity": "A publication is identified by (sourceId, canonicalUrl). A revision is identified by (publicationId, revisionHash), and each revision owns its object links.",
    "revision-ordering": "observedAt is the source observation time. An older observation never overwrites the current revision; equal timestamps use the revision hash as a deterministic tie-breaker. A date-only or timezone-less crawler timestamp is interpreted as Asia/Shanghai local time before persistence.",
    "tombstones": "Deletion is explicit: a tombstone item creates a tombstone revision for an existing publication and marks it deleted. Unknown publications cannot be tombstoned. A tombstone result is reported as updated.",
    "batch-idempotency": "Batch identity is scoped by principalKey and batchId. Replaying the same canonical payload returns the stored result without reapplying writes; reusing the pair with another digest returns 409. clientRunId is likewise scoped by principalKey.",
    "partial-item-rejection": "A source or item validation error is returned as a per-item rejected result and does not roll back other valid items in the batch. Batch-level identity, manifest, and database failures remain errors.",
    "objects": "Object keys are generated by the server as publications/{kind}/sha256/{first-two-hex}/{sha256}. Clients cannot choose keys or object metadata. A manifest is at most 32 MiB in this first slice, covering the current crawler's largest public attachments. Metadata ingestion succeeds even when an object is not yet present in R2; plan/complete verifies size, content type, kind, SHA-256 metadata, and the actual bytes (or a provider SHA-256 checksum) before moving the object to verified/linked.",
    "outbox": "Accepted revisions write a durable PublicationEventOutbox row in the same transaction. A future Queue consumer may derive work from this outbox; the ingestion path does not claim delivery until a real publisher exists.",
    "content-type": "Object manifests accept a bare MIME type such as text/html or image/png. Parameters such as charset are rejected so the signed Content-Type and verification contract remain unambiguous.",
    "required-upload-headers": "An upload plan returns the exact required Content-Type, x-amz-meta-kind, and x-amz-meta-sha256 headers. Presigned URLs are short-lived and no R2 credentials are returned."
  },
  "capabilities": {
    "publication-ingestion": {
      "title": "Crawler publication batch ingestion",
      "auth": "admin",
      "web": "unavailable",
      "graphql": "unavailable",
      "mcp": "unavailable",
      "rest": {
        "routes": [
          {
            "path": "/api/ingestion/publications/batches",
            "method": "POST",
            "returns": "{ batchId, clientRunId, payloadDigest, results[] }",
            "notes": [
              "Request and response use the shared camelCase fixture shape in docs/contracts/fixtures/publication-batch.json.",
              "Each result has status created, updated, unchanged, or rejected; rejected results include an error and nullable publicationId/revisionId."
            ]
          },
          {
            "path": "/api/ingestion/publications/objects/plan",
            "method": "POST",
            "returns": "{ batchId, objects[] }",
            "notes": [
              "Only manifests registered by the caller's accepted batch may be planned.",
              "Each object is already_present or upload_required; upload_required includes uploadUrl, expiresAt, r2Key, and requiredHeaders."
            ]
          },
          {
            "path": "/api/ingestion/publications/objects/complete",
            "method": "POST",
            "returns": "{ batchId, kind, sha256, status: linked }",
            "notes": [
              "The server resolves the key from the registered manifest and verifies R2 metadata plus the actual bytes before linking it."
            ]
          }
        ]
      },
      "display": {
        "fields": [
          "source descriptor registration",
          "publication and revision metadata",
          "per-item ingestion status",
          "content-addressed object manifest and verification status"
        ]
      }
    }
  }
}
