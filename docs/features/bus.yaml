kind: feature
id: bus
name: Bus
areas:
- catalog
- workspace
policy_refs:
- interface-hierarchy
- ui
- permission-ui
- rendering-and-cache
- cases.mcp-assistant-workflows
access:
  anon: Can query bus information.
  user: Can query bus information and save preferences.
  admin: Can maintain bus data.
  agent: Can read bus timetable capabilities and manage own bus preferences.
requirements:
- id: bus.public-web-personal-overlay
  category: privacy
  rule: The Web timetable renders public timetable data without user preferences, then fetches the private
    bus-preferences endpoint after hydration. Preference writes are disabled until this request resolves;
    authentication and read failures do not silently overwrite stored preferences. Request-time timetable
    and map SSR remain dynamic.
- id: bus.public-no-signin
  category: authorization
  rule: Bus is public campus life information; sign-in should not be a prerequisite.
- id: bus.weekday-by-shanghai-time
  category: consistency
  rule: The workspace bus page determines weekday, Saturday, or Sunday by Shanghai time by default and
    allows manual switching.
- id: bus.shanghai-time-interpretation
  category: consistency
  rule: When calculating 'how long until departure', whether a trip has departed, and route ordering,
    the workspace client interprets HH:mm times in the timetable uniformly as Shanghai Time, not changing
    with the browser's timezone.
- id: bus.core-filters-only
  category: presentation
  rule: The workspace bus page provides only core filters for departure stop, arrival stop, direction
    reversal, and 'show already-departed trips'; departure and arrival stops use a directly clickable
    vertical stop column to avoid secondary dropdowns, maintaining clear departure/arrival hierarchy.
- id: bus.visual-priority
  category: presentation
  rule: The workspace visual structure should prioritize 'quick scanning of the next trip and route differences';
    selected stop, direction, next departure time, and route grouping must be more prominent than decorative
    cards.
- id: bus.public-responsive-planner
  category: presentation
  rule: The public /catalog/bus page restores the complete two-column route planner and grouped timetable
    at desktop widths. On mobile it leads with the next boardable departure and up to two following departures,
    keeps route controls behind progressive disclosure, and shows the complete grouped timetable by default
    with an option to collapse it. A valid recent local route is reused when no signed-in preference is
    available.
- id: bus.semantic-public-route
  category: behavior
  rule: Anonymous /?tab=bus requests permanently redirect to /catalog/bus; signed-in compatibility requests
    redirect to /catalog/bus.
- id: bus.sort-by-next-boardable
  category: presentation
  rule: Routes are currently sorted by 'the next boardable time from the selected departure stop'; if
    a stop has no exact time in the original timetable, the average of adjacent known stop times in the
    same trip is used as an estimate.
- id: bus.merged-table-grouped-by-route
  category: presentation
  rule: The signed-in workspace and the public /catalog/bus default timetable use a single merged large
    table to display all applicable routes, grouped into table sections by route; each section shows the
    visible trips for that route.
- id: bus.raw-data-returned
  category: behavior
  rule: /api/catalog/bus and catalog_bus_timetable_get return raw route/stop/trip data within the selected
    timetable version; the preferences field is null for anonymous callers and returns the authenticated
    caller's saved preferences; the server does not pre-apply recommendations, filtering, or sorting.
- id: bus.version-topology-self-contained
  category: consistency
  rule: When versionKey selects a historical timetable version, routes, stops, campuses, and trips are
    derived from that same imported schedule version rather than the latest global route topology.
- id: bus.version-key-boundary
  category: consistency
  rule: REST, GraphQL, and MCP normalize surrounding whitespace in versionKey and then require 1-120 ASCII
    characters matching ^[A-Za-z0-9][A-Za-z0-9._-]*$.
- id: bus.current-version-only
  category: consistency
  rule: catalog_bus_route_list only lists routes actually present in the current active timetable version;
    it does not expose historical route IDs outside the current version.
- id: bus.workspace-vs-map
  category: behavior
  rule: The workspace prioritizes answering 'how to get the next trip'; the map page handles spatial understanding.
- id: bus.public-version-metadata-omitted
  category: presentation
  rule: Public bus workspace pages omit timetable version metadata rather than showing an unlabeled or
    unlocalized imported value; the raw title remains available to admin, REST, and MCP consumers.
- id: bus.responsive-route-surfaces
  category: presentation
  rule: Bus planner and transit-map pages keep document content within narrow viewports, provide at least
    44 CSS pixels of height for primary route actions, wrap stop sequences only between complete arrow-and-stop
    tokens, and keep campus labels legible over route strokes.
- id: bus.task-oriented-tool-hierarchy
  category: presentation
  rule: Use catalog_bus_timetable_get for raw/full datasets, catalog_bus_route_list for unfiltered discovery,
    catalog_bus_route_search for filtered discovery, catalog_bus_route_get for one route, catalog_bus_departure_next
    for immediate departure answers, and workspace_bus_preferences_get/workspace_bus_preferences_set for
    personal defaults.
- id: bus.canonical-compact-mode
  category: presentation
  rule: catalog_bus_timetable_get default mode returns compact counts, route/campus summaries, and relevant
    departures rather than the full trip dataset; the deprecated summary input is an alias for this same
    shape.
- id: bus.cache-boundaries
  category: consistency
  rule: Raw timetable REST responses remain private because they may contain viewer preferences; route-search
    REST responses may use revision-purged public edge caching, while all next-departure responses remain
    private and uncached. The bus map dynamically renders request-time active trips while reusing revision-scoped
    static schedule and topology data.
capabilities:
  bus-timetable:
    title: Bus Timetable
    auth: anon
    web:
      pages:
      - /catalog/bus
      - /catalog/bus/map
    rest:
      routes:
      - path: /api/catalog/bus
      - path: /api/workspace/bus-preferences
        method: POST
        auth: user
        notes:
        - Unknown non-null preferred origin or destination campus IDs return 400 with { error } and do
          not write preferences.
      - path: /api/workspace/bus-preferences
        auth: user
    graphql:
      queries:
      - name: busRoutes
        parent: Catalog
        rest_equivalent: GET /api/catalog/bus
        mcp_equivalent: catalog_bus_route_list
        notes:
        - The GraphQL page argument bounds the route catalog to pageSize 1-100.
      - name: busTimetable
        parent: Catalog
        rest_equivalent: GET /api/catalog/bus
        mcp_equivalent: catalog_bus_route_get
        notes:
        - The input selects one route and bounds weekday, Saturday, and Sunday trip slots to pageSize
          1-100.
        - The optional now argument is a DateTime and must include an explicit Z or numeric timezone offset.
      mutations:
      - name: busPreferencesSet
        auth: user
        required_scopes:
        - workspace.bus-preferences:write
        rest_equivalent: POST /api/workspace/bus-preferences
        mcp_equivalent: workspace_bus_preferences_set
    mcp:
      tools:
      - name: catalog_bus_timetable_get
        rest_equivalent: GET /api/catalog/bus
        notes:
        - default mode returns a compact assistant-oriented snapshot with counts, summarized campuses/routes,
          and relevant nextDepartures instead of the full trip dataset; deprecated summary is an alias
          for default.
        - Request full mode for the raw campuses/routes/trips dataset.
      - name: catalog_bus_route_list
        rest_equivalent: GET /api/catalog/bus
        notes:
        - Use this for full current-version route discovery before fetching one route timetable.
      - name: catalog_bus_route_get
        rest_equivalent: GET /api/catalog/bus
        notes:
        - Single-route raw timetable tool; use catalog_bus_departure_next when the caller only needs the
          next few departures.
      - name: workspace_bus_preferences_get
        rest_equivalent: GET /api/workspace/bus-preferences
        notes:
        - Returns the authenticated caller's saved bus preferences or the default null-campus preference.
      - name: workspace_bus_preferences_set
        rest_equivalent: POST /api/workspace/bus-preferences
        notes:
        - preferredOriginCampusId and preferredDestinationCampusId may be null to reset saved campuses.
        - Unknown non-null preferred origin or destination campus IDs return a serialized validation result
          with success=false and do not write preferences.
    presentation:
      items:
      - route.descriptionPrimary
      - next departure times
      - campus stop columns
      - route schedules
      - day type control
      - departure/arrival campus controls
      - reverse direction control
      - show-departed toggle
  bus-route-search:
    title: Bus Route Search
    auth: anon
    web: unavailable
    rest:
      routes:
      - path: /api/catalog/bus/routes
        notes:
        - Filtered route discovery helper for origin/destination clients.
        - Preserves concrete route variants rather than merging routes with the same campus pair.
    mcp:
      tools:
      - name: catalog_bus_route_search
        rest_equivalent: GET /api/catalog/bus/routes
        notes:
        - Filtered discovery helper for origin/destination prompts.
        - Use catalog_bus_route_list when the caller needs the entire current route catalog.
    presentation:
      items:
      - originCampus
      - destinationCampus
      - filtered route list
      - route stop order
      - weekday/Saturday/Sunday trip counts
  bus-next-departures:
    title: Bus Next Departures
    auth: anon
    web: unavailable
    rest:
      routes:
      - path: /api/catalog/bus/next
        auth: anon
        notes:
        - Requires originCampusId and destinationCampusId.
        - Supports atTime, dayType, includeDeparted, limit, versionKey, and locale query parameters.
        - 'REST and MCP share the same limit bounds: 1-50.'
        - Departures include status and minutesUntilDeparture so clients can mark departed rows and the
          next upcoming trip.
    mcp:
      tools:
      - name: catalog_bus_departure_next
        rest_equivalent: GET /api/catalog/bus/next
        notes:
        - Task-oriented convenience tool for 'when is the next bus?' prompts.
        - 'Default parameters: dayType=auto, includeDeparted=false, limit=5; REST and MCP share limit
          bounds 1-50.'
        - When a route exists but no remaining departures are available for the requested window, return
          guidance describing the situation and, when possible, the next available service time.
        - Default and the deprecated summary alias avoid repeating the same originCampus/destinationCampus
          object inside every departure item when they already appear at the top level.
    presentation:
      items:
      - originCampus
      - destinationCampus
      - dayType
      - departureTime
      - arrivalTime
      - minutesUntilDeparture
      - route description
  bus-map:
    title: Bus Route Map
    auth: anon
    web:
      pages:
      - /catalog/bus/map
    rest: unavailable
    mcp: unavailable
    presentation:
      items:
      - Bus routes (polylines on map)
      - Bus stops/stations (markers)
      - Active trips derived from timetable times
      - Campus locations reference
      - Interactive map controls
