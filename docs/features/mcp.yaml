kind: feature
id: mcp
name: MCP
areas:
- platform
policy_refs:
- interface-hierarchy
- cases.mcp-assistant-workflows
access:
  anon: Cannot call MCP tools that require user context.
  user: After authorization, can call personal workspace-related tools.
  admin: Currently does not automatically gain additional MCP admin tools due to admin status.
  agent: MCP is one of the protocol entry points alongside REST, covering capabilities aligned with the
    web by default while enforcing resource-bound bearer tokens with canonical feature:action scopes.
requirements:
- id: mcp.personal-workspace-focus
  category: behavior
  rule: MCP focuses by default on personal learning workspace, public query, and low-risk personal state
    write capabilities; admin capabilities are not exposed by default.
- id: mcp.text-formatted-json
  category: presentation
  rule: Every tool result is one object with a required success boolean. The text JSON and structuredContent
    representations are identical; non-object domain payloads are nested under result.
- id: mcp.output-modes
  category: presentation
  rule: MCP has one canonical compact response structure in default mode and an opt-in full mode that
    adds raw fields without changing top-level collection types. The legacy summary input remains accepted
    as a deprecated alias for default and never introduces summary wrapper objects. workspace_subscription_kind_update
    edits only an existing owner subscription. Personal subscription read outputs include kind in compact
    and full modes.
- id: mcp.coverage
  category: behavior
  rule: The checked tool list below is the MCP contract surface; implementation parity is enforced through
    integration tests and $life-ustc-implement. Public catalog list tools accept page and limit between
    1 and 100, while course, section, and teacher search strings accept 2-200 characters, matching Web,
    REST, and GraphQL.
- id: mcp.aggregate-before-fanout
  category: presentation
  rule: Prefer assistant-oriented aggregate or filtered tools first; start with workspace_snapshot_get,
    then workspace_schedule_next for next-class-only prompts and workspace_deadline_list for merged deadlines.
    Default mode compacts schedule payloads and redacts low-value nested fields; full mode is the explicit
    escape hatch. Raw dataset tools remain available for power clients that need local post-processing.
- id: mcp.privacy-safe-default
  category: privacy
  rule: Default output may omit repeated low-value nested objects and redact token-bearing URLs or other
    sensitive strings; full mode is the explicit escape hatch when exact raw values are required, except
    workspace_calendar_feed_get never returns the private personal iCal feed URL, calendar path, credential,
    or token in any mode.
- id: mcp.property-priority-parity
  category: presentation
  rule: 'MCP default output follows the shared UI Model Property Priority contract: keep primary identity/task
    fields and useful disambiguators, compact repeated nested objects, and omit low-priority raw/audit
    fields unless full mode is explicitly requested.'
- id: mcp.actionable-errors
  category: presentation
  rule: Validation and common not-found payloads prefer plain-language messages and may include a hint
    that points to the next useful tool or query to recover.
- id: mcp.resource-bound-access-token
  category: authorization
  rule: MCP is Bearer-only. The upstream @better-auth/mcp mcpHandler verifies JWT signature, issuer, expiry,
    and the canonical or loopback-equivalent /api/mcp audience. The local adapter then requires the exact
    active user-grant generation before constructing SDK AuthInfo. DPoP and resource-less opaque tokens
    are rejected. Authorization resources remain code-bound; omitted-resource refresh maps to MCP only
    with an MCP feature scope and canonical MCP approval.
- id: mcp.complete-batch-scope-enforcement
  category: authorization
  rule: Every fixed-scope tool feature declared by each requested tool is required across the complete
    JSON-RPC batch. A feature:write grant also satisfies feature:read for the same feature. The GraphQL
    runner delegates exact selected-field scopes to the shared resolvers.
- id: mcp.least-privilege-bootstrap-scope
  category: authorization
  rule: The upstream MCP verifier returns a JSON-RPC -32000 authentication error and RFC 9728 Bearer resource_metadata
    challenge for missing or invalid credentials; the adapter adds only account.profile:read as the bootstrap
    scope. Exact local per-tool insufficient_scope challenges drive incremental authorization. WWW-Authenticate
    values escape quotes and backslashes and reject control characters; descriptions never reflect request-controlled
    tool names, and scope values come only from the registry.
- id: mcp.graphql-authorization
  category: authorization
  rule: graphql_operation_run requires an MCP-resource bearer and exactly one arbitrary document or compatible
    registered operation. Arbitrary aliases and fragments cannot bypass authorization because selected
    Workspace and Account fields and mutations enforce their feature scopes in the shared resolvers. Missing
    scopes return an insufficient_scope WWW-Authenticate challenge for incremental authorization.
- id: mcp.graphql-planning-prompt
  category: presentation
  rule: plan_graphql_operation embeds the canonical GraphQL SDL and document-free operation manifest into
    the requested prompt, then instructs clients to use one bounded named operation, request only challenged
    scopes, confirm mutations explicitly, and inspect state rather than blindly retry after an ambiguous
    timeout.
- id: mcp.registered-graphql-timeout-ambiguity
  category: consistency
  rule: graphql_operation_run stops waiting after five seconds or caller cancellation, including for serial
    GraphQL mutations. A mutation already in progress may still commit, so clients must inspect current
    state and must not blindly retry after REQUEST_TIMEOUT or REQUEST_CANCELLED.
- id: mcp.registered-upload-boundary
  category: privacy
  rule: GraphQL upload mutations expose quota reservation, on-site authenticated PUT workflow metadata,
    completion of an already-uploaded object, rename, and delete. Although the runner accepts a GraphQL
    document, it never accepts or proxies R2 object bytes. Its MCP bearer remains resource-bound to /api/mcp
    and is not replayable at the REST PUT URL.
- id: mcp.bounded-auth-first-request-body
  category: validation
  rule: MCP transport requests run upstream @better-auth/mcp JWT verification and local active-grant authorization
    before parsing JSON-RPC. Authenticated JSON request bodies are streamed with a 64 KiB limit and JSON-RPC
    batches are limited to 50 messages. Empty batches return JSON-RPC -32600 Invalid Request; larger bodies
    or batches return 413 before tool handling. Accepted bodies are parsed once and shared by scope enforcement,
    observability, rate limiting, and the SDK transport.
- id: mcp.transport-observability
  category: privacy
  rule: MCP transport logging must never include bearer tokens, cookies, exception messages, or tool argument
    values.
- id: mcp.mutation-rate-limits
  category: validation
  rule: 'Ordinary MCP mutations are preflighted against the shared per-host, per-user feature-action Cloudflare
    budgets: 60 writes or 10 batch writes per minute. Registered GraphQL mutations instead use the shared
    resolver guard with the same feature:write key, avoiding duplicate consumption. Fixed-tool batches
    fail before side effects on limit or unavailable bindings; responses use 429 or 503 with Retry-After:
    60.'
- id: mcp.rate-limit-accuracy-boundary
  category: consistency
  rule: MCP mutation throttles are per-Cloudflare-location, permissive, eventually consistent abuse controls
    rather than global exact quotas. The protocol does not promise remaining/reset counters, and read-only
    tools do not consume mutation budgets.
- id: mcp.flexible-date-inputs
  category: validation
  rule: Date and datetime parameters on MCP tools accept ISO 8601 with timezone offset (2026-05-01T08:00:00+08:00),
    bare date strings (2026-05-01, treated as UTC midnight for @db.Date columns), or timezone-less datetimes
    (2026-05-01T08:00:00, interpreted as Asia/Shanghai). Invalid strings produce a descriptive error response
    rather than a validation rejection.
- id: mcp.time-override
  category: consistency
  rule: Time-sensitive tools (workspace_snapshot_get, workspace_schedule_next, workspace_calendar_timeline_get,
    workspace_deadline_list, workspace_overview_get, catalog_bus_departure_next) accept an optional atTime
    parameter to anchor their internal clock to a caller-supplied moment instead of the server clock,
    enabling reproducible queries and future-scenario planning.
- id: mcp.openai-compatible-descriptors
  category: consistency
  rule: Every registered tool advertises a title, a closed top-level object outputSchema with a required
    success boolean and typed known fields, MCP annotations for read-only/destructive/open-world hints,
    top-level securitySchemes, and _meta.securitySchemes. Ordinary tools declare fixed OAuth feature scopes;
    graphql_operation_run uses conservative static safety hints and delegates exact scopes to each manifest
    entry. Default and full results keep the same top-level key and collection types.
- id: mcp.single-tools-list-compatibility-wrapper
  category: consistency
  rule: The SDK compatibility handler that hoists _meta.securitySchemes onto each tools/list descriptor
    is installed exactly once after all application tools are registered, rather than nesting one full-list
    wrapper per tool.
- id: mcp.tool-registry-completeness
  category: consistency
  rule: Server startup fails when any registered MCP tool lacks an explicit OAuth scope entry or output
    schema, so tool registration cannot silently fall back to generic metadata.
- id: mcp.room-maps
  category: behavior
  rule: catalog_rooms_map is public, accepts code and mode, and returns room map coverage and public image
    URLs.
- id: mcp.tool-groups.comment-write-scope
  category: authorization
  rule: Ordinary signed-in user comment writes are available; admin moderation remains unavailable through
    MCP.
  applies_to:
  - tool-groups
capabilities:
  protocol-endpoint:
    title: Protocol Endpoint
    auth: user
    web: unavailable
    rest:
      routes:
      - path: /api/mcp
        method: POST
        notes:
        - All tool calls are handled through this endpoint and require a Bearer token bound to the MCP
          protected resource /api/mcp. Upstream @better-auth/mcp performs JWT resource verification while
          local authorization enforces active grants and exact tool scopes.
        - Trusted browser-origin Bearer-token clients receive CORS headers, including exposed MCP-Session-Id,
          Retry-After, and WWW-Authenticate response headers for browser transports.
        - Answers browser CORS preflight requests for the MCP transport headers used by POST/DELETE browser
          clients.
        - Requests with a foreign Origin header are rejected before MCP auth/tool handling.
      - path: /api/mcp
      - path: /api/mcp
        method: DELETE
      - path: /api/mcp
        method: OPTIONS
        auth: anon
        notes:
        - Answers browser CORS preflight requests for trusted origins before bearer-token MCP transport
          handling.
    presentation:
      items:
      - MCP protocol server endpoint
      - GET/POST/DELETE transport methods
      - Returns JSON-RPC or transport responses
  tool-groups:
    title: Tool Groups
    auth: user
    mcp:
      groups:
      - name: Profile & Todo
        tools:
        - account_profile_get
        - account_client_activity_list
        - community_user_get
        - workspace_todo_list
        - workspace_todo_create
        - workspace_todo_update
        - workspace_todo_delete
      - name: Registered GraphQL
        tools:
        - graphql_operation_run
      - name: Workspace & Overview
        tools:
        - workspace_snapshot_get
        - workspace_overview_get
        - workspace_schedule_next
        - workspace_deadline_list
        - catalog_link_list
        - workspace_link_pin_list
        - workspace_link_pin_set
      - name: Course, Section & Semester
        tools:
        - catalog_semester_list
        - catalog_semester_current
        - catalog_course_search
        - catalog_course_get
        - catalog_section_search
        - catalog_section_get
        - catalog_teacher_search
        - catalog_teacher_get
        - catalog_section_match_preview
      - name: Comments
        tools:
        - community_comment_list
        - community_comment_get
        - community_comment_create
        - community_comment_update
        - community_comment_delete
        - community_comment_replies
        - community_comment_reaction_add
        - community_comment_reaction_remove
      - name: Descriptions
        tools:
        - community_description_get
        - community_description_set
      - name: Uploads
        tools:
        - workspace_upload_list
        - workspace_upload_rename
        - workspace_upload_delete
      - name: Calendar & Subscription
        tools:
        - workspace_calendar_event_list
        - workspace_calendar_timeline_get
        - workspace_calendar_feed_get
        - workspace_subscription_list
        - workspace_subscription_add
        - workspace_subscription_remove
        - workspace_subscription_kind_update
        - catalog_section_calendar_feed_get
        - workspace_subscription_import
      - name: Homework, Schedule & Exam
        tools:
        - workspace_homework_list
        - workspace_homework_completion_set
        - workspace_schedule_list
        - workspace_exam_list
        - community_section_homework_list
        - community_section_homework_create
        - community_section_homework_update
        - community_section_homework_delete
        - catalog_schedule_list
        - catalog_section_schedule_list
        - catalog_section_exam_list
      - name: Bus
        tools:
        - catalog_bus_timetable_get
        - catalog_bus_route_list
        - catalog_bus_route_get
        - workspace_bus_preferences_get
        - workspace_bus_preferences_set
        - catalog_bus_route_search
        - catalog_bus_departure_next
      - name: Weather and room maps
        tools:
        - catalog_weather_get
        - catalog_rooms_map
      - name: Young Events
        tools:
        - catalog_young_event_list
        - catalog_young_event_get
        - catalog_young_organizer_list
        - catalog_young_organizer_get
        - workspace_young_event_subscription_list
        - workspace_young_event_subscription_get
        - workspace_young_event_subscription_set
        - workspace_young_organizer_subscription_list
        - workspace_young_organizer_subscription_get
        - workspace_young_organizer_subscription_set
        - workspace_young_notification_list
        - workspace_young_notification_read
    presentation:
      items:
      - Schedule tools
      - Homework tools
      - Section tools
      - Tool name
      - Tool title
      - Tool description
      - Tool annotations
      - OAuth security schemes
      - Input schema
      - Output schema
      - Preferred tool for common prompts
