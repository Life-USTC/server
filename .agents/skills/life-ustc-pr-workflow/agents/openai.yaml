interface:
  display_name: "Life USTC PR Workflow"
  short_description: "End-to-end repo PR development flow"
  default_prompt: "Use $life-ustc-pr-workflow to implement a repo change, verify it, and handle PR publishing/checks when requested or already in an active PR workflow."
